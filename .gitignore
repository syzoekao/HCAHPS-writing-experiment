# OS / editor cruft
.DS_Store
.vscode/
*.swp
*~

# Claude Code project-local permission state (grows as commands are approved)
.claude/settings.json
.claude/settings.local.json

# Stata / data (real data lives in OneDrive, not in repo)
*.dta
*.csv
*.dct
*.gph
*.smcl
*.log

# Python
__pycache__/
*.pyc
.venv/

# Pandoc-generated outputs (regenerable from .md)
# (reference-doc templates like pandoc-reference*.docx are kept; only generated output is ignored)
manuscript/draft.docx

# Office temp/lock files
~$*

# Sensitive
.env
*.key
*.pem

readings/

# Superseded versions kept locally for reference, not tracked
manuscript/outdated/